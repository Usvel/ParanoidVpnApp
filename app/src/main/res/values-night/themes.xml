<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Paranoid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/primaryDark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="toolBarColor">@color/white</item>
        <item name="backgroundVioletLight">@color/minorColorLightDarkTheme</item>
        <item name="backgroundViolet">@color/minorColorDarkTheme</item>
        <!-- Nav bar -->
        <item name="navBarIconColor">@color/lightNavIconSelected</item>
        <item name="navBarIconColorNotChecked">@color/lightNavIconNotSelected</item>
        <item name="toolBarColorSecond">@color/secondBackgroundTransparent</item>
        <!-- Text -->
        <item name="textTransparent">@color/lightTextTransparent</item>
        <!-- VPN Button -->
        <item name="vpnButtonDisabled">@color/vpnColorDisabledDark</item>
        <item name="vpnButtonConnected">@color/vpnColorConnected</item>
        <item name="vpnButtonError">@color/vpnColorError</item>
    </style>
</resources>