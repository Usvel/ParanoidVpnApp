package com.paranoid.vpn.app.test

class Test {

        fun Int.add(s: Int): Int{
            return this + s
        }

    companion object{
        fun Int.add3(s: Int): Int{
            return this + s
        }
    }
}


fun Int.add2(s: Int): Int{
    return this + s
}